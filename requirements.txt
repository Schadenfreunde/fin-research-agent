# ============================================================
# Python dependencies for FinResearchAgent
# ============================================================
# Install locally with: pip install -r requirements.txt
# These are automatically installed in the Docker container.
# ============================================================

# ── Google Cloud & Vertex AI ───────────────────────────────
google-cloud-aiplatform>=1.132.0      # Vertex AI SDK — includes the vertexai module
google-adk>=1.0.0                     # Google Agent Development Kit (ADK 1.x)
google-genai>=1.0.0                   # Google Generative AI SDK (pulled by ADK, listed explicitly)
google-cloud-storage>=2.14.0         # Cloud Storage client
google-cloud-secret-manager>=2.20.0  # Secret Manager (for API keys)
# NOTE: Do NOT add a separate "vertexai" package — it was discontinued after v1.71.1.
#       The vertexai module is included inside google-cloud-aiplatform.

# ── Web framework ──────────────────────────────────────────
fastapi>=0.110.0                      # Web framework
uvicorn[standard]>=0.27.0            # ASGI server for FastAPI

# ── Configuration ──────────────────────────────────────────
pyyaml>=6.0.1                         # YAML config file parsing

# ── Standard library dependencies (no install needed, listed for clarity)
# json, math, datetime, os, pathlib, urllib — all built into Python 3.11+
# google-generativeai was the old/deprecated SDK — removed; google-genai (above) is the current SDK
